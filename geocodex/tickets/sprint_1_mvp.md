# Sprint 1 — Cimientos (MVP) — Deliverables y subtasks

Duración estimada: 3 días
Owner: Developer

Deliverables
- Documento de migraciones y esquema (ready to implement)
- Documentos de modelos y políticas de acceso
- Fixtures listadas y aprobadas

Subtickets
- S1-1: `DB Schema` — definir tablas `las_files`, `las_curve_data`, columnas, índices y FKs. DoD: SQL/pseudocode aprobado.
- S1-2: `Models & Policies` — describir `LasFile`, `LasCurveData`, GlobalScopes por `tenant_id` y `LasFilePolicy`. DoD: ejemplos de queries y políticas.
- S1-3: `Fixtures` — definir y revisar fixtures `valid_2.0.las`, `broken_header.las`, `file_with_extra_curves.las`. DoD: fixtures documentadas con ejemplos.

Acceptance Criteria
- Todos los entregables son documentos en `geocodex/architecture` y `geocodex/tickets` listos para implementar.

